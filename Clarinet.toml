[project]
name = "multisig-compliance"
authors = ["Multisig Compliance Team"]
description = "A decentralized multi-signature compliance management protocol"
telemetry = false
requirements = []
[contracts.multisig-compliance-core]
path = "contracts/multisig-compliance-core.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
